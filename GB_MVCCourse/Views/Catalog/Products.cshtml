@model Catalog;

@{
	ViewData["Title"] = "Products Page";
}

@foreach (var product in Model.Products)
{
	<div>Id: @product.Id<br/>
		Name: @product.Name <br/>
		<img src=@(ViewData["imagePath"]+product.Image)/></div>
		<hr/>
}